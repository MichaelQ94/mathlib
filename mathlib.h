#ifndef MATHLIB_H
#define MATHLIB_H

namespace Math {
	unsigned gcd(unsigned, unsigned);
	int pow(int, unsigned);
	unsigned fac(unsigned);
	unsigned permute(unsigned, unsigned);
	unsigned choose(unsigned, unsigned);
	unsigned countdigits(unsigned);
	unsigned countbits(unsigned);
}

#endif
